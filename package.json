{
	"name": "spotify-svelte",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "vite dev --host 127.0.0.1 --port 8898",
		"build": "vite build",
		"preview": "vite preview",
		"deploy": "git add -A && git commit -m deploy && git push"
	},
	"devDependencies": {
		"@sveltejs/adapter-static": "^2.0.2",
		"@sveltejs/kit": "^1.20.4",
		"svelte": "^4.0.0",
		"vite": "^4.3.6"
	},
	"type": "module"
}
